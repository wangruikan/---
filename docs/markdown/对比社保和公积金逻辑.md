# 对比：社保补差 vs 公积金补差的新旧基数读取逻辑

## 📊 逻辑对比

### 社保补差（第54-85行）

```php
// 1. 查找旧基数：本月之前最近一次生效的基数
$lastAdjustmentBeforeThisMonth = BaseAdjustment::where('employee_id', $employee->id)
    ->where('status', 'applied')
    ->whereNotNull('social_security_effective_date')
    ->where('social_security_effective_date', '<', $currentMonth)  // 本月之前
    ->orderBy('social_security_effective_date', 'desc')
    ->first();

if ($lastAdjustmentBeforeThisMonth) {
    $oldBase = $lastAdjustmentBeforeThisMonth->new_social_security_base;  // ✅ 读取new字段
    $effectiveDate = Carbon::parse($lastAdjustmentBeforeThisMonth->social_security_effective_date);
} else {
    // 如果没有历史调基记录，使用员工当前基数
    $oldBase = $employee->social_security_base;  // ✅ 兜底：员工表
    $effectiveDate = Carbon::parse(Carbon::now()->year . '-01-01');
}

// 2. 查找新基数：本月是否有生效的基数
$thisMonthAdjustment = BaseAdjustment::where('employee_id', $employee->id)
    ->where('status', 'applied')
    ->whereNotNull('social_security_effective_date')
    ->whereYear('social_security_effective_date', Carbon::now()->year)
    ->whereMonth('social_security_effective_date', Carbon::now()->month)
    ->orderBy('social_security_effective_date', 'desc')
    ->first();

if ($thisMonthAdjustment) {
    // 本月有调基，使用本月的基数作为新基数
    $currentBase = $thisMonthAdjustment->new_social_security_base;  // ✅ 读取new字段
} else {
    // 本月没有调基，使用旧基数作为新基数（但会用新上下限约束）
    $currentBase = $oldBase;  // ✅ 沿用旧基数
}
```

---

### 公积金补差（第180-211行）

```php
// 1. 查找旧基数：本月之前最近一次生效的基数
$lastAdjustmentBeforeThisMonth = BaseAdjustment::where('employee_id', $employee->id)
    ->where('status', 'applied')
    ->whereNotNull('housing_fund_effective_date')
    ->where('housing_fund_effective_date', '<', $currentMonth)  // 本月之前
    ->orderBy('housing_fund_effective_date', 'desc')
    ->first();

if ($lastAdjustmentBeforeThisMonth) {
    $oldBase = $lastAdjustmentBeforeThisMonth->new_housing_fund_base;  // ✅ 读取new字段
    $effectiveDate = Carbon::parse($lastAdjustmentBeforeThisMonth->housing_fund_effective_date);
} else {
    // 如果没有历史调基记录，使用员工当前基数
    $oldBase = $employee->housing_fund_base;  // ✅ 兜底：员工表
    $effectiveDate = Carbon::parse(Carbon::now()->year . '-01-01');
}

// 2. 查找新基数：本月是否有生效的基数
$thisMonthAdjustment = BaseAdjustment::where('employee_id', $employee->id)
    ->where('status', 'applied')
    ->whereNotNull('housing_fund_effective_date')
    ->whereYear('housing_fund_effective_date', Carbon::now()->year)
    ->whereMonth('housing_fund_effective_date', Carbon::now()->month)
    ->orderBy('housing_fund_effective_date', 'desc')
    ->first();

if ($thisMonthAdjustment) {
    // 本月有调基，使用本月的基数作为新基数
    $currentBase = $thisMonthAdjustment->new_housing_fund_base;  // ✅ 读取new字段
} else {
    // 本月没有调基，使用旧基数作为新基数（但会用新上下限约束）
    $currentBase = $oldBase;  // ✅ 沿用旧基数
}
```

---

## 🔍 结论

**逻辑完全一样**，只是字段名不同：

| 步骤 | 社保 | 公积金 |
|------|------|--------|
| 查找调基记录 | `social_security_effective_date` | `housing_fund_effective_date` |
| 读取旧基数（有记录） | `new_social_security_base` | `new_housing_fund_base` |
| 读取旧基数（无记录） | `employee->social_security_base` | `employee->housing_fund_base` |
| 读取新基数（本月有调基） | `new_social_security_base` | `new_housing_fund_base` |
| 读取新基数（本月无调基） | 沿用 `$oldBase` | 沿用 `$oldBase` |

---

## 🐛 问题可能在哪里？

既然逻辑一样，那么1400可能来自：

1. **`base_adjustments` 表有旧记录**：
   - 本月之前有一条记录，`new_social_security_base = 1400`
   
2. **`employees` 表的基数是1400**：
   - 如果没有调基记录，会读取 `employees.social_security_base`

---

## 🧪 建议执行

执行 `查找1400来源.sql` 查看：
- `base_adjustments` 表中是否有 `new_social_security_base = 1400` 的记录
- `employees` 表中 `social_security_base` 是否是1400

这样就能定位1400的来源了！

