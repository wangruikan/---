-- ===================================================================
-- 立即执行：添加项目考勤设置字段
-- ===================================================================
-- 使用方法：
-- 1. 在Navicat中打开此文件
-- 2. 选择数据库（weiqing 或 hrms）
-- 3. 点击"运行"按钮
-- ===================================================================

-- 添加字段（如果已存在会报错，但不影响系统运行）
ALTER TABLE `projects` 
ADD COLUMN `require_attendance` TINYINT(1) NOT NULL DEFAULT 1 
COMMENT '是否需要考勤：1-需要，0-不需要' 
AFTER `status`;

-- 查看结果
SELECT 
    '✅ 字段添加成功！' AS result,
    '项目考勤设置功能已部署完成' AS status;

-- ===================================================================
-- 说明：
-- - 如果提示"字段已存在"，说明之前已经添加过，可以忽略
-- - 默认值为1（需要考勤），保持向后兼容
-- - 可以在项目管理中修改此设置
-- ===================================================================

