# 📊 完整测试方案 - 包含新旧上下限

## 前提条件

- **员工**：wrk
- **社保类型**：养老保险
- **公司比例**：100% (1.0)
- **个人比例**：100% (1.0)
- **当前时间**：2025年10月
- **补差期间**：2025-01 至 2025-09（9个月）

---

## 🔵 社保补差测试方案

### 方案1：基数低于新下限 ⭐ 推荐

```
┌─────────────────────────────────────────────────┐
│ 2024年状态（旧）                                 │
├─────────────────────────────────────────────────┤
│ 基数：2500                                      │
│ 上下限：2000 - 30000                            │
│ 基数约束后：2500（在范围内）                    │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│ 2025年10月修改（新）                            │
├─────────────────────────────────────────────────┤
│ 基数：2500（本月没调基，沿用）                  │
│ 上下限：3000 - 35000                            │
│ 基数约束后：3000（低于下限，用下限）            │
└─────────────────────────────────────────────────┘

补差：3000 - 2500 = 500
```

#### SQL设置
```sql
-- 社保
DELETE FROM insurance_compensation_records WHERE employee_name = 'wrk' AND compensation_type = 'social_security';
UPDATE employees SET social_security_base = 2500 WHERE name = 'wrk';

UPDATE social_security_regions 
SET 
    -- 旧上下限（2024年的标准）
    old_min_base_amount = 2000,
    old_max_base_amount = 30000,
    old_limits_updated_at = '2024-09-01',
    
    -- 新上下限（2025年的标准）
    new_min_base_amount = 3000,
    new_max_base_amount = 35000,
    new_limits_updated_at = NOW(),
    
    -- 兼容字段
    min_base_amount = 3000,
    max_base_amount = 35000
WHERE id = (SELECT social_security_region_id FROM employees WHERE name = 'wrk');
```

#### 预期结果
```
旧基数：2500.00
新基数：3000.00
补差月数：9
公司合计：4500.00（500 × 1.0 × 9）
个人合计：4500.00（500 × 1.0 × 9）
总额：9000.00
```

---

### 方案2：基数大幅提高

```
┌─────────────────────────────────────────────────┐
│ 2024年状态（旧）                                 │
├─────────────────────────────────────────────────┤
│ 基数：2000                                      │
│ 上下限：1500 - 28000                            │
│ 基数约束后：2000                                │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│ 2025年10月调基+修改上下限（新）                 │
├─────────────────────────────────────────────────┤
│ 调基：2000 → 5000                               │
│ 上下限：2360 - 30000                            │
│ 基数约束后：5000                                │
└─────────────────────────────────────────────────┘

补差：5000 - 2000 = 3000
```

#### SQL设置
```sql
DELETE FROM insurance_compensation_records WHERE employee_name = 'wrk' AND compensation_type = 'social_security';
DELETE FROM base_adjustments WHERE employee_id = (SELECT id FROM employees WHERE name = 'wrk');

UPDATE employees SET social_security_base = 5000 WHERE name = 'wrk';

-- 插入2024年调基
INSERT INTO base_adjustments VALUES (
    NULL, (SELECT id FROM employees WHERE name = 'wrk'), 1, 1,
    NULL, 2000, '2024-01-01',
    NULL, NULL, NULL,
    NULL, NULL, NULL,
    NULL, NULL, NULL,
    '2024年基数', 'applied', '2024-01-01', '2024-01-01', NOW()
);

-- 插入2025年调基
INSERT INTO base_adjustments VALUES (
    NULL, (SELECT id FROM employees WHERE name = 'wrk'), 1, 1,
    2000, 5000, '2025-10-01',
    NULL, NULL, NULL,
    NULL, NULL, NULL,
    NULL, NULL, NULL,
    '2025年调基', 'applied', '2025-10-01', NOW(), NOW()
);

-- 设置上下限
UPDATE social_security_regions 
SET 
    old_min_base_amount = 1500,
    old_max_base_amount = 28000,
    new_min_base_amount = 2360,
    new_max_base_amount = 30000,
    min_base_amount = 2360,
    max_base_amount = 30000
WHERE id = (SELECT social_security_region_id FROM employees WHERE name = 'wrk');
```

#### 预期结果
```
旧基数：2000.00
新基数：5000.00
补差月数：9
公司合计：27000.00（3000 × 1.0 × 9）
个人合计：27000.00
总额：54000.00
```

---

### 方案3：基数超出新上限（约束降低）

```
┌─────────────────────────────────────────────────┐
│ 2024年状态（旧）                                 │
├─────────────────────────────────────────────────┤
│ 基数：30000                                     │
│ 上下限：2000 - 35000                            │
│ 基数约束后：30000                               │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│ 2025年10月修改（新）                            │
├─────────────────────────────────────────────────┤
│ 基数：30000（没调基）                           │
│ 上下限：3000 - 26000（上限降低）                │
│ 基数约束后：26000（超出上限，用上限）           │
└─────────────────────────────────────────────────┘

补差：26000 - 30000 = -4000（负数）
```

#### SQL设置
```sql
DELETE FROM insurance_compensation_records WHERE employee_name = 'wrk' AND compensation_type = 'social_security';
UPDATE employees SET social_security_base = 30000 WHERE name = 'wrk';

UPDATE social_security_regions 
SET 
    old_min_base_amount = 2000,
    old_max_base_amount = 35000,
    new_min_base_amount = 3000,
    new_max_base_amount = 26000,
    min_base_amount = 3000,
    max_base_amount = 26000
WHERE id = (SELECT social_security_region_id FROM employees WHERE name = 'wrk');
```

#### 预期结果
```
旧基数：30000.00
新基数：26000.00
补差月数：9
公司合计：-36000.00（-4000 × 1.0 × 9）
个人合计：-36000.00
总额：-72000.00
```

---

## 🟢 公积金补差测试方案

### 方案1：基数低于新下限 ⭐ 推荐

```
┌─────────────────────────────────────────────────┐
│ 2024年状态（旧）                                 │
├─────────────────────────────────────────────────┤
│ 基数：1800                                      │
│ 上下限：1000 - 25000                            │
│ 基数约束后：1800                                │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│ 2025年10月修改（新）                            │
├─────────────────────────────────────────────────┤
│ 基数：1800                                      │
│ 上下限：2200 - 28000                            │
│ 基数约束后：2200（低于下限，用下限）            │
└─────────────────────────────────────────────────┘

补差：2200 - 1800 = 400
```

#### SQL设置
```sql
DELETE FROM insurance_compensation_records WHERE employee_name = 'wrk' AND compensation_type = 'housing_fund';
UPDATE employees SET housing_fund_base = 1800 WHERE name = 'wrk';

UPDATE housing_fund_configs 
SET 
    old_min_base_amount = 1000,
    old_max_base_amount = 25000,
    old_limits_updated_at = '2024-09-01',
    new_min_base_amount = 2200,
    new_max_base_amount = 28000,
    new_limits_updated_at = NOW(),
    min_base_amount = 2200,
    max_base_amount = 28000
WHERE id = (SELECT housing_fund_config_id FROM employees WHERE name = 'wrk');
```

#### 预期结果
```
旧基数：1800.00
新基数：2200.00
补差月数：9
公司合计：3600.00（400 × 1.0 × 9）
个人合计：3600.00
总额：7200.00
```

---

### 方案2：基数提高整数倍（便于计算）

```
┌─────────────────────────────────────────────────┐
│ 2024年状态（旧）                                 │
├─────────────────────────────────────────────────┤
│ 调基：基数 2000                                 │
│ 上下限：1500 - 30000                            │
│ 基数约束后：2000                                │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│ 2025年10月调基+修改（新）                       │
├─────────────────────────────────────────────────┤
│ 调基：基数 4000                                 │
│ 上下限：2000 - 35000                            │
│ 基数约束后：4000                                │
└─────────────────────────────────────────────────┘

补差：4000 - 2000 = 2000
```

#### SQL设置
```sql
DELETE FROM insurance_compensation_records WHERE employee_name = 'wrk' AND compensation_type = 'housing_fund';
DELETE FROM base_adjustments WHERE employee_id = (SELECT id FROM employees WHERE name = 'wrk');

UPDATE employees SET housing_fund_base = 4000 WHERE name = 'wrk';

-- 2024年调基
INSERT INTO base_adjustments (
    employee_id, account_set_id, created_by,
    new_housing_fund_base, housing_fund_effective_date,
    adjustment_reason, status, applied_at, created_at, updated_at
) VALUES (
    (SELECT id FROM employees WHERE name = 'wrk'), 1, 1,
    2000, '2024-01-01',
    '2024年基数', 'applied', '2024-01-01', '2024-01-01', NOW()
);

-- 2025年调基
INSERT INTO base_adjustments (
    employee_id, account_set_id, created_by,
    old_housing_fund_base, new_housing_fund_base, housing_fund_effective_date,
    adjustment_reason, status, applied_at, created_at, updated_at
) VALUES (
    (SELECT id FROM employees WHERE name = 'wrk'), 1, 1,
    2000, 4000, '2025-10-01',
    '2025年调基', 'applied', '2025-10-01', NOW(), NOW()
);

-- 设置上下限
UPDATE housing_fund_configs 
SET 
    old_min_base_amount = 1500,
    old_max_base_amount = 30000,
    new_min_base_amount = 2000,
    new_max_base_amount = 35000,
    min_base_amount = 2000,
    max_base_amount = 35000
WHERE id = (SELECT housing_fund_config_id FROM employees WHERE name = 'wrk');
```

#### 预期结果
```
旧基数：2000.00
新基数：4000.00
补差月数：9
公司合计：18000.00（2000 × 1.0 × 9）
个人合计：18000.00
总额：36000.00
```

---

## 📊 测试数据速查表

### 社保补差

| 方案 | 2024年基数 | 2024年上下限 | 2025年基数 | 2025年上下限 | 约束后旧基数 | 约束后新基数 | 差额 | 总额(×18) |
|------|-----------|-------------|-----------|-------------|-------------|-------------|------|----------|
| S1 | 2500 | 2000-30000 | 2500 | 3000-35000 | 2500 | 3000 | 500 | 9000 |
| S2 | 2000 | 1500-28000 | 5000 | 2360-30000 | 2000 | 5000 | 3000 | 54000 |
| S3 | 30000 | 2000-35000 | 30000 | 3000-26000 | 30000 | 26000 | -4000 | -72000 |
| S4 | 1000 | 800-25000 | 3000 | 2000-30000 | 1000 | 3000 | 2000 | 36000 |

### 公积金补差

| 方案 | 2024年基数 | 2024年上下限 | 2025年基数 | 2025年上下限 | 约束后旧基数 | 约束后新基数 | 差额 | 总额(×18) |
|------|-----------|-------------|-----------|-------------|-------------|-------------|------|----------|
| H1 | 1800 | 1000-25000 | 1800 | 2200-28000 | 1800 | 2200 | 400 | 7200 |
| H2 | 2000 | 1500-30000 | 4000 | 2000-35000 | 2000 | 4000 | 2000 | 36000 |
| H3 | 6000 | 2000-30000 | 3000 | 2500-25000 | 6000 | 3000 | -3000 | -54000 |
| H4 | 500 | 300-20000 | 500 | 1000-22000 | 500 | 1000 | 500 | 9000 |

---

## 🎯 推荐测试组合

### 组合1：简单验证（整百数）

**社保 S1**：
- 旧：2500（上下限2000-30000）→ 2500
- 新：2500（上下限3000-35000）→ 3000
- 差额：500
- **总额：9000**

**公积金 H1**：
- 旧：1800（上下限1000-25000）→ 1800
- 新：1800（上下限2200-28000）→ 2200
- 差额：400
- **总额：7200**

### 组合2：大额验证（整千数）

**社保 S2**：
- 旧：2000 → 2000
- 新：5000 → 5000
- 差额：3000
- **总额：54000**

**公积金 H2**：
- 旧：2000 → 2000
- 新：4000 → 4000
- 差额：2000
- **总额：36000**

### 组合3：负数验证（降低）

**社保 S3**：
- 旧：30000 → 30000
- 新：30000 → 26000（约束）
- 差额：-4000
- **总额：-72000**

**公积金 H3**：
- 旧：6000 → 6000
- 新：3000 → 3000
- 差额：-3000
- **总额：-54000**

---

## 🚀 一键设置脚本

### 社保方案S1
```sql
DELETE FROM insurance_compensation_records WHERE employee_name = 'wrk' AND compensation_type = 'social_security';
UPDATE employees SET social_security_base = 2500 WHERE name = 'wrk';
UPDATE social_security_regions 
SET old_min_base_amount = 2000, old_max_base_amount = 30000,
    new_min_base_amount = 3000, new_max_base_amount = 35000,
    min_base_amount = 3000, max_base_amount = 35000
WHERE id = (SELECT social_security_region_id FROM employees WHERE name = 'wrk');

-- 预期：旧2500，新3000，总额9000
```

### 公积金方案H1
```sql
DELETE FROM insurance_compensation_records WHERE employee_name = 'wrk' AND compensation_type = 'housing_fund';
UPDATE employees SET housing_fund_base = 1800 WHERE name = 'wrk';
UPDATE housing_fund_configs 
SET old_min_base_amount = 1000, old_max_base_amount = 25000,
    new_min_base_amount = 2200, new_max_base_amount = 28000,
    min_base_amount = 2200, max_base_amount = 28000
WHERE id = (SELECT housing_fund_config_id FROM employees WHERE name = 'wrk');

-- 预期：旧1800，新2200，总额7200
```

---

## 📝 验证清单

### 每个方案需要验证

- [ ] 旧基数正确（约束后的值）
- [ ] 新基数正确（约束后的值）
- [ ] 补差期间：2025-01 ~ 2025-09
- [ ] 补差月数：9
- [ ] 公司合计 = 差额 × 1.0 × 9
- [ ] 个人合计 = 差额 × 1.0 × 9
- [ ] 总额 = 差额 × 18

---

## 💡 快速验证技巧

**100%比例下的快速公式**：
```
总额 = 基数差额 × 18

例如：
差额 500 → 总额 9000 ✅
差额 3000 → 总额 54000 ✅
差额 -4000 → 总额 -72000 ✅
```

只需要验证：
1. 基数约束是否正确
2. 总额 = 差额 × 18

---

现在您有了完整的新旧上下限数据，可以全面测试了！🎯

