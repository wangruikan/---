# ✅ 修复完成 - 只计算员工实际参保的险种

## 🐛 问题描述

之前的补差逻辑会计算**该地区下所有险种**，导致：
- 如果地区有2个险种（如养老+失业），总额会是预期的2倍
- 即使员工只参保了1个险种，也会计算2个

**例如**：
```
差额：400
地区有2个险种（养老100%+失业100%）
但员工只参保了养老保险

旧逻辑：
  养老：400×1.0×9 = 3600（公司） + 3600（个人） = 7200
  失业：400×1.0×9 = 3600（公司） + 3600（个人） = 7200
  总额：14400 ❌

新逻辑：
  养老：400×1.0×9 = 3600（公司） + 3600（个人） = 7200
  总额：7200 ✅
```

---

## ✅ 修复方案

修改 `BaseLimitCompensationService.php`，从 `insurance_personnel` 表读取员工**实际参保的险种**：

### 旧代码（错误）

```php
case 'social_security':
    // ❌ 获取地区下所有险种
    $types = $employee->socialSecurityRegion->socialSecurityTypes;
    foreach ($types as $type) {
        // 计算所有险种...
    }
```

### 新代码（正确）

```php
case 'social_security':
    // ✅ 从参保人员表获取员工实际参保的险种
    $personnel = InsurancePersonnel::where('employee_id', $employee->id)
        ->where('status', 'active')
        ->first();
    
    if ($personnel && $personnel->social_security_types) {
        // 解析参保险种配置
        $enrolledTypes = is_string($personnel->social_security_types) 
            ? json_decode($personnel->social_security_types, true) 
            : $personnel->social_security_types;
        
        if (is_array($enrolledTypes)) {
            foreach ($enrolledTypes as $typeConfig) {
                // 只计算员工实际参保的险种
                $companyRatio = floatval($typeConfig['company_ratio'] ?? 0);
                $employeeRatio = floatval($typeConfig['employee_ratio'] ?? 0);
                
                $companyAmount = $baseDiff * $companyRatio * $compensationMonths;
                $personalAmount = $baseDiff * $employeeRatio * $compensationMonths;
                
                $compensationDetails[] = [
                    'name' => $typeConfig['name'] ?? '未知险种',
                    'company_amount' => round($companyAmount, 2),
                    'personal_amount' => round($personalAmount, 2),
                    'company_ratio' => $companyRatio,
                    'employee_ratio' => $employeeRatio
                ];
                
                $companyTotal += $companyAmount;
                $personalTotal += $personalAmount;
            }
        }
    }
```

---

## 📊 `insurance_personnel.social_security_types` 字段格式

这个字段存储员工实际参保的险种配置（JSON格式）：

```json
[
    {
        "id": 1,
        "name": "养老保险",
        "company_ratio": 1.0,
        "employee_ratio": 1.0
    }
]
```

---

## 🧪 测试步骤

### 1. 清理旧数据
```sql
DELETE FROM insurance_compensation_records WHERE employee_name = 'wrk';
```

### 2. 查看员工实际参保的险种
```sql
SELECT 
    ip.employee_name AS '员工',
    ip.social_security_types AS '参保险种配置'
FROM insurance_personnel ip
WHERE ip.employee_name = 'wrk'
AND ip.status = 'active';
```

### 3. 在前端修改社保上下限

触发补差计算。

### 4. 验证结果

```sql
SELECT 
    employee_name,
    compensation_type,
    old_base,
    new_base,
    company_total,
    personal_total,
    total_amount,
    compensation_details
FROM insurance_compensation_records
WHERE employee_name = 'wrk';
```

**预期**：
- 如果员工只参保1个险种（养老100%）
- 差额400，月数9
- 总额 = 400 × 1.0 × 9 × 2（公司+个人）= **7200** ✅

---

## 📝 核心逻辑

```
只计算员工实际参保的险种 = 
  从 insurance_personnel.social_security_types 读取 + 
  按配置中的比例计算
```

---

## 🎯 验证公式

```
总额 = 差额 × 比例 × 月数 × 2（公司+个人）

例如：
  差额：400
  比例：1.0（100%）
  月数：9
  总额 = 400 × 1.0 × 9 × 2 = 7200 ✅
```

---

现在请执行 `clean_and_test.sql` 清理数据，然后在前端重新触发补差！🚀

