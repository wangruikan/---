# ✅ 保险明细 - 按险种分列展示

## 📋 需求

保险明细按照Excel格式展示，每个险种独立成列：

```
养老保险 | 医疗保险 | 失业保险 | 公积金 | 大额医疗
```

每个险种下包含：基数、单位（比例+金额）、个人（比例+金额）

---

## 🎯 表头结构

### Excel格式对照

```
┌─────────┬─────────┬─────────┬────────┬──────────┐
│ 养老保险 │ 医疗保险 │ 失业保险 │ 公积金  │ 大额医疗  │
├─────────┼─────────┼─────────┼────────┼──────────┤
│ 基数    │ 基数    │ 基数    │ 基数   │ 单位     │
│ 单位    │ 单位    │ 单位    │ 单位   │ 个人     │
│  ├比例  │  ├比例  │  ├比例  │  ├比例 │          │
│  └金额  │  └金额  │  └金额  │  └金额 │          │
│ 个人    │ 个人    │ 个人    │ 个人   │          │
│  ├比例  │  ├比例  │  ├比例  │  ├比例 │          │
│  └金额  │  └金额  │  └金额  │  └金额 │          │
└─────────┴─────────┴─────────┴────────┴──────────┘
```

### 实际显示效果

| 姓名 | 养老保险-基数 | 养老保险-单位比例 | 养老保险-单位金额 | 养老保险-个人比例 | 养老保险-个人金额 | 医疗保险-基数 | ... |
|------|--------------|------------------|------------------|------------------|------------------|--------------|-----|
| 张三 | ¥7,947.00    | 16.00%           | ¥1,271.52        | 8.00%            | ¥635.76          | ¥7,948.00    | ... |

---

## 💻 核心实现

### 1. 动态生成险种列

```vue
<template v-for="(type, typeIndex) in getInsuranceTypes(salaryDetails)" :key="'insurance-' + typeIndex">
  <el-table-column :label="type.name" align="center">
    <el-table-column label="基数" width="90" align="right">
      <template #default="{ row }">
        {{ getInsuranceValue(row, type.key, 'base') }}
      </template>
    </el-table-column>
    <el-table-column label="单位" align="center">
      <el-table-column label="比例" width="70" align="right">
        <template #default="{ row }">
          {{ getInsuranceValue(row, type.key, 'company_ratio') }}
        </template>
      </el-table-column>
      <el-table-column label="金额" width="90" align="right">
        <template #default="{ row }">
          {{ getInsuranceValue(row, type.key, 'company_amount') }}
        </template>
      </el-table-column>
    </el-table-column>
    <el-table-column label="个人" align="center">
      <el-table-column label="比例" width="70" align="right">
        <template #default="{ row }">
          {{ getInsuranceValue(row, type.key, 'personal_ratio') }}
        </template>
      </el-table-column>
      <el-table-column label="金额" width="90" align="right">
        <template #default="{ row }">
          {{ getInsuranceValue(row, type.key, 'personal_amount') }}
        </template>
      </el-table-column>
    </el-table-column>
  </el-table-column>
</template>
```

### 2. 提取所有险种类型

```javascript
// 获取所有险种类型（从所有员工的保险明细中提取）
const getInsuranceTypes = (details) => {
  if (!details || details.length === 0) return []
  
  const typesSet = new Set()
  details.forEach(row => {
    if (row.insurance_details && row.insurance_details.social_security) {
      row.insurance_details.social_security.forEach(item => {
        typesSet.add(JSON.stringify({ name: item.name, key: item.name }))
      })
    }
  })
  
  return Array.from(typesSet).map(str => JSON.parse(str))
}
```

**说明**：
- 遍历所有员工的保险明细
- 提取所有出现过的险种名称（去重）
- 返回险种列表，用于动态生成表格列

### 3. 获取指定险种的值

```javascript
// 获取指定险种的值
const getInsuranceValue = (row, typeKey, field) => {
  if (!row.insurance_details || !row.insurance_details.social_security) return '-'
  
  const item = row.insurance_details.social_security.find(i => i.name === typeKey)
  if (!item) return '-'
  
  const value = item[field]
  if (value === undefined || value === null) return '-'
  
  // 格式化不同类型的值
  if (field.includes('ratio')) {
    return (value * 100).toFixed(2) + '%'
  } else if (field.includes('amount') || field === 'base') {
    return formatMoney(value)
  }
  
  return value
}
```

**说明**：
- 根据险种名称查找对应的保险数据
- 根据字段类型（比例/金额/基数）进行格式化
- 如果员工没有该险种，显示 `-`

---

## 📊 完整列顺序

```
序号 | 姓名 | 部门 | 岗位 | 基本工资 | 津贴 | 加班费 | 奖金 | 应发工资
↓
养老保险 (基数 | 单位比例 | 单位金额 | 个人比例 | 个人金额)
↓
医疗保险 (基数 | 单位比例 | 单位金额 | 个人比例 | 个人金额)
↓
失业保险 (基数 | 单位比例 | 单位金额 | 个人比例 | 个人金额)
↓
... (其他社保险种)
↓
公积金 (基数 | 单位比例 | 单位金额 | 个人比例 | 个人金额)
↓
大额医疗 (单位金额 | 个人金额)
↓
单位保险合计 | 个人保险合计 | 补差合计 | 个税 | 实发工资 | ...
```

---

## ✨ 特性

### 1. **动态险种列**
- 自动识别项目中所有员工的险种
- 只显示实际存在的险种
- 不同项目可能有不同的险种组合

### 2. **灵活适配**
- 员工A参保：养老+医疗+失业
- 员工B参保：养老+医疗
- 表格自动显示：养老、医疗、失业三列
- 员工B在"失业"列显示 `-`

### 3. **格式化显示**
- 基数：¥7,947.00
- 比例：16.00%
- 金额：¥1,271.52
- 空值：-

---

## 🎯 与Excel格式对比

### Excel表头
```
(6月)单位部分
├─ 社保基数
├─ 公积金基数
├─ 大额基数
├─ 医疗基数
├─ 社保细分1
├─ 社保细分2
├─ 公积金
└─ 大额医疗
```

### 系统表头
```
养老保险 | 医疗保险 | 失业保险 | 公积金 | 大额医疗
```

**优势**：
- ✅ 更清晰：每个险种独立成列
- ✅ 更灵活：自动适配不同的险种组合
- ✅ 更直观：基数、比例、金额一目了然
- ✅ 易导出：可直接导出为标准Excel格式

---

## 🚀 测试

### 1. 刷新浏览器
```
Ctrl + F5
```

### 2. 查看工资明细
```
1. 进入"薪金计算"
2. 选择已生成的工资表
3. 点击"查看明细"
4. ✅ 应该看到每个险种独立成列
```

### 3. 验证显示
- ✅ 养老保险、医疗保险等各自独立成列
- ✅ 每列包含：基数、单位（比例+金额）、个人（比例+金额）
- ✅ 公积金和大额医疗也独立成列
- ✅ 没有参保的险种显示 `-`

---

## 📸 预期效果

```
┌────┬────┬──────────────────────────────────────────────────────────────┐
│序号│姓名│ 养老保险                    │ 医疗保险                      │
│    │    ├──────┬─────────┬─────────┼──────┬─────────┬─────────────┤
│    │    │基数  │单位     │个人     │基数  │单位     │个人         │
│    │    │      │比例|金额│比例|金额│      │比例|金额│比例|金额    │
├────┼────┼──────┼─────────┼─────────┼──────┼─────────┼─────────────┤
│ 1  │张三│7947  │16% |1271│8%  |636 │7948  │8%  |636 │2%   |159   │
└────┴────┴──────┴─────────┴─────────┴──────┴─────────┴─────────────┘
```

---

## 🎉 完成！

现在保险明细按照Excel格式，每个险种独立成列展示！
- ✅ 养老保险单独一列
- ✅ 医疗保险单独一列
- ✅ 失业保险单独一列
- ✅ 公积金单独一列
- ✅ 大额医疗单独一列
- ✅ 每列包含完整的基数、比例、金额信息

