# 🚀 补差筛选测试指南

## 📊 当前数据状态

```
wrk 员工的补差记录:

社保补差 (ID: 27):
  补差期间: 2025-01 ~ 2025-09
  创建时间: 2025-10-25
  创建月份: 2025-10 ← 按这个筛选！
  单位: -¥4,950.45
  个人: -¥4,950.45

公积金补差 (ID: 26):
  补差期间: 2025-01 ~ 2025-09
  创建时间: 2025-10-24
  创建月份: 2025-10 ← 按这个筛选！
  单位: ¥3,600.00
  个人: ¥3,600.00
```

## ✅ 测试步骤

### 1. 参保增减页面测试

#### 步骤：
1. 打开"参保增减"页面
2. 切换到"社保补交明细"标签
3. 选择月份：**2025-10**
4. 点击"查询"按钮

#### 预期结果：
- ✅ 显示 wrk 的社保补差记录
- ✅ 显示补差期间：2025-01 至 2025-09
- ✅ 显示补差月数：9
- ✅ 显示单位合计：-¥4,950.45
- ✅ 显示个人合计：-¥4,950.45

#### 负面测试（选择其他月份）：
- 选择 **2025-09** → ❌ 不应该显示（创建于10月，不是9月）
- 选择 **2025-11** → ❌ 不应该显示（创建于10月，不是11月）

### 2. 工资表测试（不同逻辑）

#### 步骤：
1. 打开"工资管理"页面
2. 选择月份：**2025-09**（补差期间内）
3. 生成工资表
4. 查看 wrk 员工的补差列

#### 预期结果：
- ✅ 单位部分显示：社保补差 -¥4,950.45，公积金补差 ¥3,600.00
- ✅ 个人部分显示：社保补差 -¥4,950.45，公积金补差 ¥3,600.00
- ✅ 补差合计：-¥2,700.90

#### 负面测试：
- 选择 **2025-10** 月生成工资表 → ❌ 不应该显示补差（因为补差期间是 2025-01 ~ 2025-09）

## 🎯 关键区别

| 场景 | 筛选逻辑 | 筛选字段 | 示例 |
|------|---------|---------|------|
| **参保增减 - 补交明细** | 按创建日期 | `created_at` | 选择2025-10 → 显示10月创建的记录 |
| **工资管理 - 补差数据** | 按补差时间段 | `compensation_start_month` ~ `compensation_end_month` | 生成2025-09工资 → 显示09月有效的补差 |

## 📋 验证清单

### 参保增减页面
- [ ] 选择2025-10月，能看到 wrk 的补差记录
- [ ] 选择2025-09月，看不到 wrk 的补差记录
- [ ] 不选择月份，能看到所有补差记录
- [ ] 公积金补交明细同样按创建日期筛选

### 工资管理页面
- [ ] 生成2025-09月工资表，能看到 wrk 的补差数据
- [ ] 生成2025-10月工资表，看不到 wrk 的补差数据
- [ ] 补差列在单位部分和个人部分正确显示

## 🔍 调试方法

### 查看补差记录的创建时间

```sql
SELECT 
    id,
    employee_name,
    compensation_type,
    compensation_start_month,
    compensation_end_month,
    DATE_FORMAT(created_at, '%Y-%m') as created_month,
    created_at,
    company_total,
    personal_total
FROM insurance_compensation_records
WHERE employee_name = 'wrk'
ORDER BY id DESC;
```

### 测试筛选SQL

```sql
-- 按创建日期筛选（参保增减）
SELECT * FROM insurance_compensation_records
WHERE employee_name = 'wrk'
  AND YEAR(created_at) = 2025
  AND MONTH(created_at) = 10;

-- 按补差时间段筛选（工资表）
SELECT * FROM insurance_compensation_records
WHERE employee_name = 'wrk'
  AND compensation_start_month <= '2025-09'
  AND compensation_end_month >= '2025-09';
```

## 💡 如果看不到数据

### 参保增减页面
1. 确认选择的月份是 **2025-10**
2. 刷新页面（Ctrl + F5）
3. 查看浏览器控制台，确认API返回数据

### 工资管理页面
1. 确认选择的月份是 **2025-09**（补差期间内）
2. 重新生成工资表
3. 刷新页面（Ctrl + F5）

## 🎉 完成！

现在两个页面的筛选逻辑都正确了：
- ✅ 参保增减：按创建日期筛选
- ✅ 工资管理：按补差时间段筛选

**快去测试吧！** 🚀

