# ✅ 跳过发起人审批 - 修复说明

## 🎯 问题说明

### 原来的流程
```
发起人(e1) 提交审批
    ↓
第1步：经办(e1) - 待审批 ⏳  ← 发起人还要审批自己？❌
    ↓
第2步：复核(e2) - 等待中 ⏸️
    ↓
第3步：审核(e3) - 等待中 ⏸️
```

**问题：** 发起人提交审批后，还要自己审批自己，这不合理！

### 修复后的流程
```
发起人(e1) 提交审批
    ↓
第1步：经办(e1) - 自动通过 ✅  ← 系统自动标记为已审批
    ↓
第2步：复核(e2) - 待审批 ⏳  ← 直接进入第二步
    ↓
第3步：审核(e3) - 等待中 ⏸️
```

**优点：** 发起人提交后，直接进入第二个审批人的待办，无需自己审批自己！

---

## 🔧 修复内容

### 1. 后端代码修改

**文件：** `app/Http/Controllers/SalaryApprovalController.php`

**修改位置：** `completeSubmission()` 方法

**修改前：**
```php
// 创建审批流程实例
$instance = $this->approvalService->createApprovalInstance(
    $approval->account_set_id,
    '工资表审批',
    $approval->id,
    $approval->submitted_by,
    $attachments,
    false // 不跳过发起人 ❌
);
```

**修改后：**
```php
// 创建审批流程实例
$instance = $this->approvalService->createApprovalInstance(
    $approval->account_set_id,
    '工资表审批',
    $approval->id,
    $approval->submitted_by,
    $attachments,
    true // 跳过发起人，直接进入第二步审批 ✅
);
```

### 2. 审批服务逻辑（已有）

`ApprovalService::createApprovalInstance()` 中已经实现了跳过发起人的逻辑：

```php
// 如果跳过发起人且当前审批人是发起人，则直接标记为已审批
if ($skipInitiator && $approver->user_id == $createdBy) {
    $status = 'approved';
} else {
    $status = ($stepOrder === 1) ? 'pending' : 'waiting';
}

ApprovalRecord::create([
    'instance_id' => $instance->id,
    'step_order' => $stepOrder,
    'step_name' => $approver->level_name,
    'approver_id' => $approver->user_id,
    'approver_name' => $approver->user_name,
    'status' => $status,
    'approved_at' => $status === 'approved' ? now() : null,
]);

// 如果跳过发起人，自动推进到下一步
if ($skipInitiator) {
    $this->autoAdvanceToNextStep($instance->id);
}
```

---

## 🗂️ 修复现有数据

### 问题
已经提交的工资表审批（instance_id: 103, 104）还处于第一步状态，需要手动推进到第二步。

### 解决方案：执行SQL脚本

**文件：** `fix_existing_salary_approvals.sql`

#### 步骤1：检查需要修复的数据

```sql
SELECT 
    ai.id AS instance_id,
    ai.business_type,
    ai.current_step,
    ai.total_steps,
    ai.created_by AS creator_id,
    ar1.approver_id AS step1_approver_id,
    ar1.status AS step1_status,
    ar2.approver_id AS step2_approver_id,
    ar2.status AS step2_status
FROM approval_instances ai
LEFT JOIN approval_records ar1 ON ai.id = ar1.instance_id AND ar1.step_order = 1
LEFT JOIN approval_records ar2 ON ai.id = ar2.instance_id AND ar2.step_order = 2
WHERE ai.business_type = '工资表审批'
AND ai.status = 'pending'
AND ai.created_by = ar1.approver_id  -- 第一步审批人就是发起人
AND ar1.status = 'pending';
```

**预期结果：**
```
instance_id | business_type | current_step | step1_status | step2_status
103         | 工资表审批    | 1            | pending      | waiting
104         | 工资表审批    | 1            | pending      | waiting
```

#### 步骤2：执行修复脚本

```sql
-- 2.1 标记第一步为已审批（自动通过）
UPDATE approval_records ar
JOIN approval_instances ai ON ar.instance_id = ai.id
SET 
    ar.status = 'approved',
    ar.approved_at = NOW(),
    ar.comment = '系统自动通过（发起人）'
WHERE ai.business_type = '工资表审批'
AND ai.status = 'pending'
AND ar.step_order = 1
AND ar.status = 'pending'
AND ai.created_by = ar.approver_id;

-- 2.2 更新审批实例，推进到第二步
UPDATE approval_instances ai
SET 
    ai.current_step = 2,
    ai.updated_at = NOW()
WHERE ai.business_type = '工资表审批'
AND ai.status = 'pending'
AND ai.current_step = 1;

-- 2.3 将第二步状态改为 pending（进入待审批）
UPDATE approval_records ar
JOIN approval_instances ai ON ar.instance_id = ai.id
SET ar.status = 'pending'
WHERE ai.business_type = '工资表审批'
AND ai.status = 'pending'
AND ar.step_order = 2
AND ar.status = 'waiting';
```

#### 步骤3：验证修复结果

```sql
SELECT 
    ai.id AS instance_id,
    ai.business_type,
    ai.current_step,
    ai.total_steps,
    CONCAT(u1.name, ' (ID:', ar1.approver_id, ')') AS step1_approver,
    ar1.status AS step1_status,
    ar1.approved_at AS step1_approved_at,
    CONCAT(u2.name, ' (ID:', ar2.approver_id, ')') AS step2_approver,
    ar2.status AS step2_status
FROM approval_instances ai
LEFT JOIN approval_records ar1 ON ai.id = ar1.instance_id AND ar1.step_order = 1
LEFT JOIN users u1 ON ar1.approver_id = u1.id
LEFT JOIN approval_records ar2 ON ai.id = ar2.instance_id AND ar2.step_order = 2
LEFT JOIN users u2 ON ar2.approver_id = u2.id
WHERE ai.business_type = '工资表审批'
ORDER BY ai.id DESC;
```

**预期结果：**
```
instance_id | current_step | step1_approver | step1_status | step1_approved_at   | step2_approver | step2_status
103         | 2            | e1 (ID:5)      | approved     | 2025-10-29 00:00:00 | e2 (ID:6)      | pending
104         | 2            | e1 (ID:5)      | approved     | 2025-10-29 00:00:00 | e2 (ID:6)      | pending
```

---

## 📊 修复后的效果

### 1. "我的待办"（e1 用户）
- ❌ **不再显示**工资表审批（因为已自动通过第一步）
- ✅ 只显示其他类型的待审批事项

### 2. "我的待办"（e2 用户）
- ✅ **显示工资表审批**（第二步，复核）
- ✅ e2 可以直接审批

### 3. "我发起的"（e1 用户）
- ✅ 显示工资表审批
- ✅ 状态：审批中（第2/3步）
- ✅ 当前审批人：e2（复核）

---

## 🎯 完整操作步骤

### 1️⃣ 清除缓存

```bash
php artisan config:clear
php artisan cache:clear
```

### 2️⃣ 执行SQL修复现有数据

在数据库管理工具（如 phpMyAdmin、Navicat 等）中执行 `fix_existing_salary_approvals.sql` 中的SQL语句。

**或者使用命令行：**
```bash
# Windows PowerShell
Get-Content fix_existing_salary_approvals.sql | mysql -u root -p weiqing
```

### 3️⃣ 刷新浏览器

- e1 用户：查看"我的待办"，应该不再显示工资表审批
- e2 用户：查看"我的待办"，应该显示工资表审批

### 4️⃣ 测试新提交的审批

1. 以 e1 身份提交一个新的工资表审批
2. 检查是否直接进入 e2 的待办列表
3. e2 审批通过后，进入 e3 的待办列表

---

## ✅ 验证清单

### 后端验证

```bash
# 查看审批流程状态
php artisan tinker --execute="
\$instances = \App\Models\ApprovalInstance::where('business_type', '工资表审批')
    ->with(['records' => function(\$q) { \$q->orderBy('step_order'); }])
    ->get();
foreach(\$instances as \$i) {
    echo 'Instance ' . \$i->id . ': Step ' . \$i->current_step . '/' . \$i->total_steps . PHP_EOL;
    foreach(\$i->records as \$r) {
        echo '  Step ' . \$r->step_order . ': ' . \$r->approver_name . ' - ' . \$r->status . PHP_EOL;
    }
}
"
```

**预期输出：**
```
Instance 103: Step 2/3
  Step 1: e1 - approved
  Step 2: e2 - pending
  Step 3: e3 - waiting
Instance 104: Step 2/3
  Step 1: e1 - approved
  Step 2: e2 - pending
  Step 3: e3 - waiting
```

### 前端验证

- [ ] e1 用户"我的待办"中不显示工资表审批
- [ ] e2 用户"我的待办"中显示工资表审批
- [ ] e1 用户"我发起的"中显示工资表审批（状态：审批中）
- [ ] 提交新的工资表审批，直接进入 e2 的待办

---

## 🎉 总结

### 修改内容

1. ✅ 修改 `SalaryApprovalController::completeSubmission()`
   - 将 `skipInitiator` 参数从 `false` 改为 `true`

2. ✅ 创建 SQL 脚本修复现有数据
   - 标记第一步为已审批
   - 推进到第二步
   - 激活第二步待办

### 效果

- ✅ 发起人提交审批后，自动跳过自己审批的环节
- ✅ 直接进入第二个审批人的待办列表
- ✅ 流程更加合理，符合实际业务需求

### 注意事项

- ⚠️ 必须执行 SQL 脚本来修复已提交的审批记录
- ⚠️ 新提交的审批会自动应用新逻辑
- ⚠️ 清除浏览器缓存后刷新页面

---

现在您可以：
1. 执行 SQL 脚本修复现有数据
2. 刷新浏览器
3. 切换到 e2 用户查看待办

应该就能看到工资表审批在 e2 的待办列表中了！🎊

