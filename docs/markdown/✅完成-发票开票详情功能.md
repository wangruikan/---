# ✅ 完成 - 发票开票详情功能

## 📋 需求

在发票申请编辑时，业务人员需要录入详细的开票信息，包括：

1. **基本信息**：所属期（年月）、单位名称、申请日期
2. **开票方式**：全额、缺额、无
3. **开票种类**：默认普票，可后续更改
4. **金额信息**：扣除额、税率、不含税金额、开票税额、税金
5. **开票信息**：开票日期、是否完成、开票人、发票号码
6. **备注**：开票备注信息

---

## ✅ 实现方案

### 数据结构

新增16个字段到 `invoice_applications` 表：

| 字段名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `period_year` | INT | NULL | 所属期-年份 |
| `period_month` | INT | NULL | 所属期-月份 |
| `company_name` | VARCHAR(200) | NULL | 单位名称 |
| `application_date` | DATE | NULL | 申请日期（当日）|
| `invoice_method` | ENUM | NULL | 开票方式：full/partial/none |
| `invoice_type` | VARCHAR(50) | '普票' | 开票种类 |
| `deduction_amount` | DECIMAL(15,2) | 0.00 | 扣除额 |
| `tax_rate` | DECIMAL(5,4) | 0.0000 | 税率（如0.06=6%）|
| `amount_excluding_tax` | DECIMAL(15,2) | 0.00 | 不含税金额 |
| `invoice_tax_amount` | DECIMAL(15,2) | 0.00 | 开票税额 |
| `tax_amount` | DECIMAL(15,2) | 0.00 | 税金 |
| `invoice_date` | DATE | NULL | 开票日期 |
| `is_completed` | BOOLEAN | FALSE | 是否完成 |
| `invoicer` | VARCHAR(100) | NULL | 开票人 |
| `invoice_number` | VARCHAR(100) | NULL | 发票号码 |
| `invoice_remark` | TEXT | NULL | 开票备注 |

---

## 🔧 实现内容

### 1. 数据库迁移

#### 文件：`database/migrations/2025_11_03_000006_add_invoice_details_to_invoice_applications.php`

```php
Schema::table('invoice_applications', function (Blueprint $table) {
    // 所属期
    $table->integer('period_year')->nullable()->after('project_name')->comment('所属期-年份');
    $table->integer('period_month')->nullable()->after('period_year')->comment('所属期-月份');
    
    // 单位名称
    $table->string('company_name', 200)->nullable()->after('period_month')->comment('单位名称');
    
    // 申请日期
    $table->date('application_date')->nullable()->after('company_name')->comment('申请日期');
    
    // 开票方式
    $table->enum('invoice_method', ['full', 'partial', 'none'])->nullable()->after('application_date')->comment('开票方式：full-全额，partial-缺额，none-无');
    
    // 开票种类
    $table->string('invoice_type', 50)->default('普票')->after('invoice_method')->comment('开票种类：普票、专票等');
    
    // 金额相关字段
    $table->decimal('deduction_amount', 15, 2)->default(0)->after('invoice_type')->comment('扣除额');
    $table->decimal('tax_rate', 5, 4)->default(0)->after('deduction_amount')->comment('税率（如0.06表示6%）');
    $table->decimal('amount_excluding_tax', 15, 2)->default(0)->after('tax_rate')->comment('不含税金额');
    $table->decimal('invoice_tax_amount', 15, 2)->default(0)->after('amount_excluding_tax')->comment('开票税额');
    $table->decimal('tax_amount', 15, 2)->default(0)->after('invoice_tax_amount')->comment('税金');
    
    // 开票信息
    $table->date('invoice_date')->nullable()->after('tax_amount')->comment('开票日期');
    $table->boolean('is_completed')->default(false)->after('invoice_date')->comment('是否完成');
    $table->string('invoicer', 100)->nullable()->after('is_completed')->comment('开票人');
    $table->string('invoice_number', 100)->nullable()->after('invoicer')->comment('发票号码');
    
    // 备注
    $table->text('invoice_remark')->nullable()->after('invoice_number')->comment('开票备注');
});
```

---

### 2. 模型更新

#### 文件：`app/Models/InvoiceApplication.php`

**添加到 fillable**：
```php
protected $fillable = [
    // ... 原有字段
    // 开票详细信息
    'period_year',
    'period_month',
    'company_name',
    'application_date',
    'invoice_method',
    'invoice_type',
    'deduction_amount',
    'tax_rate',
    'amount_excluding_tax',
    'invoice_tax_amount',
    'tax_amount',
    'invoice_date',
    'is_completed',
    'invoicer',
    'invoice_number',
    'invoice_remark',
];
```

**添加类型转换**：
```php
protected $casts = [
    // ... 原有字段
    'period_year' => 'integer',
    'period_month' => 'integer',
    'application_date' => 'date',
    'deduction_amount' => 'decimal:2',
    'tax_rate' => 'decimal:4',
    'amount_excluding_tax' => 'decimal:2',
    'invoice_tax_amount' => 'decimal:2',
    'tax_amount' => 'decimal:2',
    'invoice_date' => 'date',
    'is_completed' => 'boolean',
];
```

---

### 3. 控制器 - 更新开票详情API

#### 文件：`app/Http/Controllers/InvoiceApplicationController.php`

**新增方法**：
```php
/**
 * 更新开票详细信息
 */
public function updateInvoiceDetails($id, Request $request)
{
    $application = InvoiceApplication::find($id);

    if (!$application) {
        return response()->json([
            'success' => false,
            'message' => '申请不存在'
        ], 404);
    }

    // 只有可编辑状态才能修改
    if (!$application->canEdit()) {
        return response()->json([
            'success' => false,
            'message' => '当前状态不允许修改'
        ], 400);
    }

    $validator = Validator::make($request->all(), [
        'period_year' => 'nullable|integer|min:2000|max:2100',
        'period_month' => 'nullable|integer|min:1|max:12',
        'company_name' => 'nullable|string|max:200',
        'application_date' => 'nullable|date',
        'invoice_method' => 'nullable|in:full,partial,none',
        'invoice_type' => 'nullable|string|max:50',
        'deduction_amount' => 'nullable|numeric|min:0',
        'tax_rate' => 'nullable|numeric|min:0|max:1',
        'amount_excluding_tax' => 'nullable|numeric|min:0',
        'invoice_tax_amount' => 'nullable|numeric|min:0',
        'tax_amount' => 'nullable|numeric|min:0',
        'invoice_date' => 'nullable|date',
        'is_completed' => 'nullable|boolean',
        'invoicer' => 'nullable|string|max:100',
        'invoice_number' => 'nullable|string|max:100',
        'invoice_remark' => 'nullable|string',
    ]);

    if ($validator->fails()) {
        return response()->json([
            'success' => false,
            'message' => '验证失败',
            'errors' => $validator->errors()
        ], 422);
    }

    $application->update($request->only([
        'period_year',
        'period_month',
        'company_name',
        'application_date',
        'invoice_method',
        'invoice_type',
        'deduction_amount',
        'tax_rate',
        'amount_excluding_tax',
        'invoice_tax_amount',
        'tax_amount',
        'invoice_date',
        'is_completed',
        'invoicer',
        'invoice_number',
        'invoice_remark',
    ]));

    return response()->json([
        'success' => true,
        'message' => '更新成功',
        'data' => $application
    ]);
}
```

---

### 4. 路由配置

#### 文件：`routes/api.php`

```php
Route::put('/{id}/update-invoice-details', [InvoiceApplicationController::class, 'updateInvoiceDetails']); // 更新开票详情
```

---

### 5. 前端页面 - 开票详情Tab

#### 文件：`src/views/InvoiceApplications/index.vue`

**新增Tab**：
```vue
<el-tab-pane label="开票详情" name="invoice_details">
  <div class="invoice-details-section">
    <el-form :model="invoiceDetailsForm" label-width="120px" :disabled="!canEdit">
      <el-row :gutter="20">
        <!-- 所属期 -->
        <el-col :span="12">
          <el-form-item label="所属期">
            <el-row :gutter="10">
              <el-col :span="12">
                <el-select v-model="invoiceDetailsForm.period_year" placeholder="年份">
                  <el-option v-for="year in years" :key="year" :label="year + '年'" :value="year" />
                </el-select>
              </el-col>
              <el-col :span="12">
                <el-select v-model="invoiceDetailsForm.period_month" placeholder="月份">
                  <el-option v-for="month in 12" :key="month" :label="month + '月'" :value="month" />
                </el-select>
              </el-col>
            </el-row>
          </el-form-item>
        </el-col>

        <!-- 单位名称 -->
        <el-col :span="12">
          <el-form-item label="单位名称">
            <el-input v-model="invoiceDetailsForm.company_name" placeholder="请输入单位名称" />
          </el-form-item>
        </el-col>

        <!-- 申请日期 -->
        <el-col :span="12">
          <el-form-item label="申请日期">
            <el-date-picker v-model="invoiceDetailsForm.application_date" type="date" />
          </el-form-item>
        </el-col>

        <!-- 开票方式 -->
        <el-col :span="12">
          <el-form-item label="开票方式">
            <el-select v-model="invoiceDetailsForm.invoice_method">
              <el-option label="全额" value="full" />
              <el-option label="缺额" value="partial" />
              <el-option label="无" value="none" />
            </el-select>
          </el-form-item>
        </el-col>

        <!-- 开票种类 -->
        <el-col :span="12">
          <el-form-item label="开票种类">
            <el-input v-model="invoiceDetailsForm.invoice_type" placeholder="默认：普票" />
          </el-form-item>
        </el-col>

        <!-- 金额字段... -->
        <!-- 税率字段... -->
        <!-- 开票信息字段... -->

        <!-- 保存按钮 -->
        <el-col :span="24" style="text-align: right" v-if="canEdit">
          <el-button type="primary" @click="handleSaveInvoiceDetails">保存开票详情</el-button>
        </el-col>
      </el-row>
    </el-form>
  </div>
</el-tab-pane>
```

**表单数据结构**：
```javascript
const invoiceDetailsForm = reactive({
  period_year: null,
  period_month: null,
  company_name: '',
  application_date: null,
  invoice_method: null,
  invoice_type: '普票',
  deduction_amount: 0,
  tax_rate: 0,
  amount_excluding_tax: 0,
  invoice_tax_amount: 0,
  tax_amount: 0,
  invoice_date: null,
  is_completed: false,
  invoicer: '',
  invoice_number: '',
  invoice_remark: ''
})
```

**保存方法**：
```javascript
const handleSaveInvoiceDetails = async () => {
  try {
    const response = await request({
      url: `/invoice-applications/${currentApplication.value.id}/update-invoice-details`,
      method: 'put',
      data: invoiceDetailsForm
    })

    if (response.success) {
      ElMessage.success('保存成功')
      await loadApplicationDetail(currentApplication.value.id)
    }
  } catch (error) {
    console.error('保存失败', error)
    ElMessage.error(error.response?.data?.message || '保存失败')
  }
}
```

---

## 📊 使用流程

### 业务人员操作流程

1. **创建开票任务**
   - 审批人员（级别2、3、4）创建发票申请
   - 此时只需填写基本信息（任务名称、年月、项目等）

2. **业务人员编辑发起流程**
   - 点击"编辑"按钮进入详情页
   - 切换到"开票详情"Tab
   - 填写详细的开票信息：
     - 所属期（年月）
     - 单位名称
     - 申请日期（通常为当日）
     - 开票方式（全额/缺额/无）
     - 开票种类（默认普票）
     - 扣除额
     - 税率（如6%输入0.06）
     - 不含税金额
     - 开票税额
     - 税金
     - 开票日期
     - 是否完成
     - 开票人
     - 发票号码
     - 备注

3. **保存开票详情**
   - 点击"保存开票详情"按钮
   - 系统验证并保存数据

4. **提交审批**
   - 填写完扣除明细、上传附件、填写开票详情后
   - 点击"提交审批"发起审批流程

---

## 🧪 测试步骤

### 测试1：基本功能

1. **创建申请**
   - 创建一个新的发票申请 ✅
   
2. **编辑开票详情**
   - 点击"编辑"
   - 切换到"开票详情"Tab ✅
   - 填写所有字段 ✅
   - 点击"保存开票详情" ✅
   - 提示"保存成功" ✅

3. **查看开票详情**
   - 关闭对话框，重新打开 ✅
   - 切换到"开票详情"Tab ✅
   - 所有填写的数据正确显示 ✅

### 测试2：权限控制

1. **可编辑状态**
   - 正常状态：可以编辑 ✅
   - 红冲且已驳回状态：可以编辑 ✅

2. **不可编辑状态**
   - 审批中：表单禁用 ✅
   - 已通过：表单禁用 ✅

### 测试3：数据验证

1. **年份验证**
   - 输入1999：❌ 提示年份不能小于2000
   - 输入2101：❌ 提示年份不能大于2100

2. **月份验证**
   - 输入0：❌ 提示月份必须在1-12之间
   - 输入13：❌ 提示月份必须在1-12之间

3. **税率验证**
   - 输入-0.01：❌ 提示税率不能为负
   - 输入1.5：❌ 提示税率不能超过100%

4. **数值精度**
   - 金额字段：保留2位小数 ✅
   - 税率字段：保留4位小数 ✅

---

## 📋 修改文件清单

### 数据库
- ✅ `database/migrations/2025_11_03_000006_add_invoice_details_to_invoice_applications.php` - 添加开票详情字段
- ✅ `✅创建表-发票开票详情字段.sql` - SQL脚本

### 后端
- ✅ `app/Models/InvoiceApplication.php`
  - 添加16个新字段到 fillable
  - 添加类型转换到 casts

- ✅ `app/Http/Controllers/InvoiceApplicationController.php`
  - 新增 `updateInvoiceDetails()` 方法
  - 添加字段验证规则

- ✅ `routes/api.php`
  - 添加 `/invoice-applications/{id}/update-invoice-details` 路由

### 前端
- ✅ `src/views/InvoiceApplications/index.vue`
  - 新增"开票详情"Tab
  - 添加 `invoiceDetailsForm` 表单数据
  - 添加 `loadInvoiceDetailsForm()` 填充方法
  - 添加 `handleSaveInvoiceDetails()` 保存方法
  - 所有16个字段的表单控件

### 文档
- ✅ `✅完成-发票开票详情功能.md`（本文件）

---

## 🎉 完成状态

所有功能已完成！✅

- [x] 数据库字段添加（16个字段）
- [x] 模型字段配置
- [x] 控制器API实现
- [x] 路由配置
- [x] 前端Tab和表单
- [x] 数据加载和保存
- [x] 字段验证
- [x] 权限控制
- [x] 文档编写完成

现在业务人员可以在编辑发票申请时，填写完整的开票详细信息了！🎊

**使用提示**：
1. 创建开票任务后，点击"编辑"按钮
2. 切换到"开票详情"Tab
3. 填写所有必要的开票信息
4. 点击"保存开票详情"
5. 完成后即可提交审批

