# ⚡ 立即执行 - 修复发票状态显示为空的问题

## 🔍 问题描述

创建发票申请后，状态显示为空。

## 📋 原因分析

数据库表 `invoice_applications` 中：
- `status` 字段的默认值是 `'draft'`
- 但代码中创建时设置的是 `'normal'`
- 如果创建时没有显式设置状态，就会使用数据库默认值 `'draft'`
- 而前端可能没有处理 `'draft'` 状态的显示

## ✅ 解决方案

### 方案1：手动执行SQL（推荐）

```sql
-- 修改status字段的默认值为 'normal'
ALTER TABLE `invoice_applications` 
MODIFY COLUMN `status` VARCHAR(20) NOT NULL DEFAULT 'normal' 
COMMENT '状态：draft-草稿，normal-正常，pending-审批中，approved-已通过，rejected-已驳回，red_flushed-红冲';
```

**执行步骤**：
1. 打开数据库管理工具（如phpMyAdmin、Navicat等）
2. 选择对应的数据库
3. 执行上面的SQL语句
4. 刷新页面测试

---

### 方案2：使用准备好的脚本

#### Windows环境

```bash
# 方法1：运行bat脚本（可能不工作）
✅执行-更新发票状态默认值.bat

# 方法2：直接用mysql命令
mysql -u用户名 -p密码 数据库名 < database/migrations/2025_11_03_000002_update_invoice_applications_default_status.sql
```

#### Linux环境

```bash
mysql -u用户名 -p密码 数据库名 < database/migrations/2025_11_03_000002_update_invoice_applications_default_status.sql
```

---

### 方案3：验证代码是否正确设置状态

确认控制器代码中已经设置状态：

```php
// app/Http/Controllers/InvoiceApplicationController.php - store() 方法
$application = InvoiceApplication::create([
    'account_set_id' => $accountSetId,
    'application_no' => $applicationNo,
    'task_name' => $request->input('task_name'),
    'year' => $request->input('year'),
    'month' => $request->input('month'),
    'status' => InvoiceApplication::STATUS_NORMAL,  // ← 确认这行存在
    'submitter_id' => $user->id,
    'created_by' => $user->id,
]);
```

✅ **已确认**：代码中已正确设置 `status` 为 `STATUS_NORMAL`

---

## 🧪 测试步骤

1. 执行上面的SQL语句
2. 刷新浏览器，清除缓存
3. 创建一个新的发票申请
4. ✅ 检查状态列是否显示"正常"标签

---

## 📊 状态值对照表

| 数据库值 | 显示文本 | 标签颜色 |
|---------|---------|---------|
| `draft` | 草稿 | info（灰色） |
| `normal` | **正常** | primary（蓝色） |
| `pending` | 审批中 | warning（黄色） |
| `approved` | 已通过 | success（绿色） |
| `rejected` | 已驳回 | danger（红色） |
| `red_flushed` | 红冲 | danger（红色） |

---

## 🔧 如果还是不显示

### 检查1：查看数据库中的实际值

```sql
-- 查看最新创建的记录
SELECT id, application_no, status, created_at 
FROM invoice_applications 
ORDER BY created_at DESC 
LIMIT 5;
```

### 检查2：查看前端状态映射

确认 `getStatusType` 方法包含 `normal` 状态：

```javascript
// src/views/InvoiceApplications/index.vue
const getStatusType = (status) => {
  const typeMap = {
    draft: 'info',
    normal: 'primary',    // ← 确认这行存在
    pending: 'warning',
    approved: 'success',
    rejected: 'danger',
    red_flushed: 'danger'
  }
  return typeMap[status] || 'info'
}
```

### 检查3：清除浏览器缓存

1. 按 `Ctrl + Shift + Delete` 打开清除缓存对话框
2. 选择"缓存的图片和文件"
3. 点击"清除数据"
4. 刷新页面

---

## 📁 相关文件

- `database/migrations/2025_11_03_000002_update_invoice_applications_default_status.sql` - SQL脚本
- `✅执行-更新发票状态默认值.bat` - Windows执行脚本
- `app/Http/Controllers/InvoiceApplicationController.php` - 控制器（已修改）
- `app/Models/InvoiceApplication.php` - 模型（已添加STATUS_NORMAL）

---

## 🎯 快速修复命令

**最简单的方法**（直接在数据库管理工具中执行）：

```sql
ALTER TABLE `invoice_applications` MODIFY COLUMN `status` VARCHAR(20) NOT NULL DEFAULT 'normal';
```

执行完成后，新创建的申请状态就会正确显示为"正常"了！✅

