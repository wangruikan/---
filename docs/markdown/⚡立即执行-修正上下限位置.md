# ⚡ 立即执行 - 修正上下限位置

## 🎯 目标

将上下限字段从类型表移到地区表。

## 📝 执行步骤

### 步骤1：执行修正SQL

```bash
# Windows PowerShell
Get-Content fix_limits_location.sql | mysql -u用户名 -p数据库名

# 或者直接在数据库工具中执行 fix_limits_location.sql
```

### 步骤2：验证修正结果

```sql
-- 查看社保地区表（应该有新旧上下限字段）
DESC social_security_regions;

-- 查看社保类型表（应该没有新旧上下限字段）
DESC social_security_types;

-- 查看数据
SELECT id, name, min_base_amount, new_min_base_amount 
FROM social_security_regions;
```

### 步骤3：清理Laravel缓存

```bash
php artisan config:clear
php artisan route:clear
php artisan view:clear
```

## ✅ 预期结果

### social_security_regions 表应该有：
- `min_base_amount` (原字段)
- `max_base_amount` (原字段)
- `old_min_base_amount` (新增)
- `old_max_base_amount` (新增)
- `old_limits_updated_at` (新增)
- `new_min_base_amount` (新增)
- `new_max_base_amount` (新增)
- `new_limits_updated_at` (新增)

### social_security_types 表应该没有：
- ❌ 没有 `old_min_base_amount`
- ❌ 没有 `new_min_base_amount`
- ✅ 只有 `min_base_amount` 和 `max_base_amount`（可能有，但不使用）

## 📋 下一步

执行完SQL后，我需要继续修改：
1. ✅ 模型文件
2. ✅ Controller文件
3. ✅ 测试脚本

请确认SQL执行成功后告诉我，我继续修改代码！

